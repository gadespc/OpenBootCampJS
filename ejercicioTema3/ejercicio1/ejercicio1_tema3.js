// recorrer numeros del 1 al 100 mediante bucle y mostrar si el numero es par o impar

for (let i = 1; i <= 100; i++) {
    if (i % 2 == 0) {
        console.log(`El numero ${i} es par`);
    } else {
        console.log(`El numero ${i} es impar`);
    }
}